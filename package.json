{
  "name": "ui-elements",
  "version": "1.1.1",
  "description": "A repo of ui elements",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && gulp webpack && gulp copy",
    "build:dev": "rimraf dist && gulp webpack-dev && gulp copy",
    "lint": "eslint .",
    "installs": "gulp installs",
    "copy": "gulp copy",
    "dev": "npm run build:dev && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node index.js"
  },
  "keywords": [
    "localnerve",
    "ui",
    "elements"
  ],
  "author": "Alex Grant (@localnerve)",
  "license": "BSD-3-Clause",
  "dependencies": {
    "express": "^4.14.1"
  },
  "devDependencies": {
    "babel-core": "^6.22.1",
    "babel-loader": "^6.2.10",
    "babel-preset-es2015": "^6.22.0",
    "eslint": "^3.14.1",
    "eslint-config-airbnb-base": "^11.0.1",
    "eslint-plugin-import": "^2.2.0",
    "gulp": "^3.9.1",
    "q": "^1.4.1",
    "rimraf": "^2.5.4",
    "webpack": "^2.2.1"
  },
  "engines": {
    "node": ">= 6.x"
  }
}
